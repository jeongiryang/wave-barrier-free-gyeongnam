/**
 * 접근로 설명 원문 표시(스펙 16)의 1단계 고정 문구.
 *
 * `KorWithService2/detailWithTour2`의 `route` 필드는 접근로에 대한 설명
 * 문장을 담고, 경사·턱에 대한 언급이 있는 경우가 있다. 두 지점의 고도 차이로
 * 경사를 계산하지 않는다(AGENTS.md는 만들어낸 값을 금지한다). 제공처가 직접
 * 준 원문만 인용 형태로 그대로 보여주고, 이 문구로 원문이 경사·기울기를
 * 실측한 값이 아님을 항상 함께 알린다.
 *
 * 이 모듈은 순수 문구만 담는다. 네트워크·저장소·위치 API·고도(표고) API를
 * 참조하지 않는다.
 */
export const SLOPE_DESCRIPTION_DISCLAIMER = "경사와 턱의 정도는 등록된 설명 그대로예요. 실제 기울기는 확인되지 않았어요.";
