# CLAUDE.md

W.A.V.E 저장소에서 작업할 때 따르는 규칙입니다.

## 작업 단위와 PR

**작업 단위 하나에 PR 하나.** 여러 수정을 한 PR에 몰아넣지 않습니다.

작업 단위는 "이것만 되돌려도 말이 되는 변경"입니다. 예를 들어 대비 문제 수정,
인트로 애니메이션 추가, 특정 API 오류 처리는 각각 별개의 작업 단위입니다.
리뷰어가 한 번에 이해할 수 없는 크기면 이미 너무 큽니다.

### 순서

1. **최신 main을 먼저 확인한다.**
   ```bash
   git fetch origin
   git log --oneline -10 origin/main
   ```
   작업을 시작하기 전에 main에 무엇이 바뀌었는지 본다. 이미 고쳐졌거나,
   의도적으로 되돌린 것을 다시 넣는 일을 막기 위해서다.

2. **origin/main에서 브랜치를 딴다.**
   ```bash
   git switch -c <타입>/<내용> origin/main
   ```

3. **한 가지만 고친다.** 지나가다 발견한 다른 문제는 고치지 말고 적어 둔다.
   그것은 다음 작업 단위다.

4. **검증한다.** 아래 세 가지가 모두 통과해야 PR을 올린다.
   ```bash
   npm run lint
   npm test
   npm run build:vercel
   ```
   화면을 바꿨다면 실제로 렌더링해서 눈으로 확인한다. 특히 1440px와
   960px(창 절반) 두 폭에서 본다.

5. **PR을 올린다.** 본문에는 무엇을 왜 고쳤는지와 어떻게 확인했는지를 쓴다.

6. **하나가 끝나면 다음 작업 단위로 넘어간다.** 다시 1번부터.

### 브랜치 이름

| 타입 | 쓰임 |
| --- | --- |
| `feat/` | 새 기능 |
| `fix/` | 버그 수정 |
| `chore/` | 설정, 문서, 빌드 |
| `refactor/` | 동작 변화 없는 구조 개선 |

---

## 되돌리면 안 되는 결정

main에 이미 반영된 의도적인 결정들입니다. 모르고 되돌리기 쉬우니 먼저 확인합니다.

- **계정 로그인 폼을 두지 않는다.** (#12)
  이메일·비밀번호 입력 폼이 Google Safe Browsing에서 피싱으로 오탐되어
  경고가 떴습니다. `AccountMenu`는 어느 화면에서도 불러오지 않습니다.
  로그인 UI를 다시 넣어야 한다면 Safe Browsing 대응 방법을 먼저 정하고,
  별도 PR로 근거와 함께 올립니다.

- **배포는 Vercel Production 하나로만 한다.**
  `vercel.json`에서 Vercel 자체 Git 배포를 끄고 GitHub Actions CD로만 배포합니다.
  중복 배포를 막기 위한 설정이므로 되돌리지 않습니다.

- **한국관광공사 OpenAPI 응답은 저장하지 않는다.**
  실시간 호출이 원칙입니다. Neon에는 서비스가 만든 데이터만 넣습니다.

- **사용자 GPS 좌표는 브라우저 밖으로 내보내지 않는다.**
  서버·DB·공유 링크 어디에도 담기지 않습니다.
  `tests/repository-policy.test.mjs`가 이 규칙들을 지킵니다.

---

## 화면을 만들 때

[`docs/design-system.md`](docs/design-system.md)를 따릅니다. 토큰을 먼저 찾아 쓰고,
없을 때만 새로 만들되 문서에도 함께 추가합니다.

특히 이 서비스는 "갈 수 있는지"를 확인하러 오는 사람이 씁니다.
보기 좋음보다 읽히는지가 먼저입니다.

- 본문·라벨 대비 4.5:1 이상. 큰 제목과 비활성 요소만 3:1까지.
- 사진 위에 글자를 얹으면 반드시 스크림을 깐다.
- 색만으로 상태를 말하지 않는다. 글자나 아이콘을 함께 쓴다.
- 조작 대상은 최소 44px.
- 애니메이션은 `prefers-reduced-motion: reduce`에서 멈춘다.

**어두운 구역을 밝게 바꿀 때는 안쪽 글자색도 함께 바꾼다.**
배경만 바꿔 흰 글자가 흰 배경 위에 남은 사고가 있었습니다.

---

## 환경

- Node.js `22.13.0` 이상
- `.env.local`과 실제 인증키는 절대 커밋하지 않습니다.
- 저장소 경로에 한글이 있으면 Vite가 모듈 URL을 인코딩하면서 React 인스턴스가
  갈라져 모든 페이지가 500으로 죽습니다. ASCII 경로에서 개발 서버를 띄웁니다.
