# CLAUDE.md

W.A.V.E 저장소에서 작업할 때 따르는 규칙입니다.

## 자율 진행

**지시가 따로 없으면 멈추지 않고 계속 진행한다.** 한 작업 단위가 끝나면
보고하고 기다리는 것이 아니라 다음 작업 단위를 찾아 바로 이어간다.

**판단이 필요하면 스스로 결정한다.** 선택지를 늘어놓고 묻지 않는다.
가장 낫다고 판단한 안으로 진행하고, 무엇을 왜 골랐는지 PR 본문에 남긴다.
되돌리기 어려운 일(외부 서비스 설정 변경, 데이터 삭제, 도메인·키 교체)만
예외로 먼저 확인한다.

**권한을 받은 범위에서는 PR을 끝까지 처리한다.** 병합 권한이 위임된 작업은
최신 `main` 반영, 전체 로컬 검사와 새 HEAD의 CI 성공을 확인한 뒤 병합한다.
실패·대기 중 검사는 우회하지 않고, 관리자 바이패스는 리뷰 승인 규칙에만 쓴다.
독립적인 다음 작업은 `origin/main`에서 시작할 수 있지만, 선행 PR의 결과가 필요한
작업은 그 PR이 병합된 최신 `main`에서 시작해 충돌과 중복을 피한다.

**승인된 범위의 완료 조건을 충족할 때까지 진행한다.** 무한히 모든 오류를
찾았다고 주장하지 않는다. 확인한 코드·화면·검사 범위와 발견하지 못할 수 있는
제약을 함께 기록하고, 범위 밖 문제는 새 이슈로 분리한다.

### 작업 사이클

```
작업 진행 → 검증 → PR 생성 → 최신 main 재반영 → 새 CI → 병합 → 이슈 확인 → 반복
```

한 바퀴가 끝날 때마다 `git fetch origin`과 `gh issue list`, `gh pr list`를
다시 확인한다. 그 사이에 main이 움직였거나 새 이슈가 올라왔을 수 있다.

### 무엇을 다음 작업으로 고를지

아래 순서로 찾는다. 위쪽이 급하다.

1. **동작을 깨뜨리는 버그** — 콘솔 오류, 실패 요청, 잘못된 값, 무한 대기 상태
2. **접근성 결함** — 대비 미달, 라벨 없는 조작 대상, 초점 이동 불가
3. **저장소에 올라온 이슈** — `gh issue list`로 확인하고 이 프로젝트에 맞게 반영
4. **품질 저하 요소** — 타입 오류, 죽은 코드, 어긋난 정렬, 반응형 붕괴
5. **고도화** — 위 네 가지가 모두 정리된 뒤

### 이슈를 작업할 때

이슈 본문을 그대로 구현하지 않는다. 이 저장소의 제약(무장애 우선, Safe Browsing,
Vercel 단일 배포, 실시간 OpenAPI 호출)에 맞게 다시 해석해서 반영한다.
이슈와 다르게 구현했다면 왜 그렇게 했는지 PR에 적고 `Refs #번호`로 연결한다.

## 작업 단위와 PR

**작업 단위 하나에 PR 하나.** 여러 수정을 한 PR에 몰아넣지 않습니다.

작업 단위는 "이것만 되돌려도 말이 되는 변경"입니다. 예를 들어 대비 문제 수정,
인트로 애니메이션 추가, 특정 API 오류 처리는 각각 별개의 작업 단위입니다.
리뷰어가 한 번에 이해할 수 없는 크기면 이미 너무 큽니다.

### 순서

1. **최신 main을 먼저 확인한다.**
   ```bash
   git fetch origin
   git log --oneline -10 origin/main
   ```
   작업을 시작하기 전에 main에 무엇이 바뀌었는지 본다. 이미 고쳐졌거나,
   의도적으로 되돌린 것을 다시 넣는 일을 막기 위해서다.

2. **origin/main에서 브랜치를 딴다.**
   ```bash
   git switch -c <타입>/<내용> origin/main
   ```

3. **한 가지만 고친다.** 지나가다 발견한 다른 문제는 고치지 말고 적어 둔다.
   그것은 다음 작업 단위다.

4. **검증한다.** 아래 네 가지가 모두 통과해야 PR을 올린다.
   ```bash
   npm run lint
   npm run typecheck
   npm test
   npm run build:vercel
   ```
   화면을 바꿨다면 실제로 렌더링해서 눈으로 확인한다. 특히 1440px와
   960px(창 절반) 두 폭에서 본다.

5. **AI 작업 로그를 남기고 PR을 올린다.**
   [`docs/ai-logs/TEMPLATE.md`](docs/ai-logs/TEMPLATE.md)를 기준으로 PR별 로그를
   만들고, 본문에는 무엇을 왜 고쳤는지와 어떻게 확인했는지를 쓴다.

   PR 생성 직후 작성자를 assignee로 지정한다. 작성자가 아닌 기본 협업자
   `syt83`, `unknownamed`를 모두 reviewer로 요청하고, 저장소에 이미 있는 라벨 중
   변경 성격에 맞는 라벨을 하나 이상 붙인다. PR 작성자 본인은 reviewer로 넣지 않는다.
   닫힌 과거 PR은 GitHub가 허용하는 assignee·label만 보완하고 무의미한 리뷰 요청은 하지 않는다.

6. **하나가 끝나면 다음 작업 단위로 넘어간다.** 다시 1번부터.

### 병합 직전 최신 main 검증

PR 작성자가 누구든 리뷰·병합 직전에 반드시 다시 최신 `main`을 확인한다. 문서만
바꾼 PR도 예외가 아니다.

```bash
git fetch origin main
git merge-base --is-ancestor origin/main HEAD
```

두 번째 명령이 실패하면 `origin/main`을 PR 브랜치에 병합하거나 리베이스한다.
그 뒤 typecheck, lint, test, Vercel 빌드를 처음부터 다시 실행하고, 갱신된 HEAD의
GitHub CI가 성공해야 한다. 이전 커밋의 성공 결과는 재사용하지 않는다.

- 검사 실패·대기 중 상태를 관리자 권한으로 우회하지 않는다.
- 관리자 바이패스는 필수 리뷰 승인 규칙에만 사용한다.
- squash merge가 최신 `main` 위에 적용된다는 이유로 브랜치 최신화와 재검증을
  생략하지 않는다.

### 이슈 검토

- 새 이슈는 담당자와 기본 라벨을 지정한 뒤 최신 `main`을 기준으로 중복·적합성·보안·개인정보·모바일·접근성 영향을 검토한다.
- 검토 결과와 구현·보류·종결 판단의 근거를 이슈 코멘트로 남긴다.
- 적합한 이슈만 최신 `main`에서 새 브랜치를 만들고, 기존 PR 검토 절차와 동일한 검증을 거친다.
- 자동 분류 결과가 실제 내용과 다르면 구현 전에 라벨을 바로잡는다.

### 브랜치 이름

| 타입 | 쓰임 |
| --- | --- |
| `feat/` | 새 기능 |
| `fix/` | 버그 수정 |
| `chore/` | 설정, 문서, 빌드 |
| `refactor/` | 동작 변화 없는 구조 개선 |

---

## 되돌리면 안 되는 결정

main에 이미 반영된 의도적인 결정들입니다. 모르고 되돌리기 쉬우니 먼저 확인합니다.

- **계정은 별도 정식 화면에서만 제공하고 핵심 여행 기능을 막지 않는다.** (#12 이력)
  과거 헤더 모달형 이메일·비밀번호 폼은 Google Safe Browsing 피싱 오탐 이후
  제거했다. 복원된 `/login`, `/register`는 W.A.V.E 전용 계정임을 명시하고 Neon
  Auth의 same-origin 경로만 사용한다. 비밀번호를 서비스 DB에 저장하지 않으며,
  의심스러운 외부 리디렉션을 추가하지 않는다. 관광 검색·추천·지도·일정은 항상
  비로그인으로 이용할 수 있어야 한다.

- **배포는 Vercel Production 하나로만 한다.**
  `vercel.json`에서 Vercel 자체 Git 배포를 끄고 GitHub Actions CD로만 배포합니다.
  중복 배포를 막기 위한 설정이므로 되돌리지 않습니다.

- **한국관광공사 OpenAPI 응답은 저장하지 않는다.**
  실시간 호출이 원칙입니다. Neon에는 서비스가 만든 데이터만 넣습니다.

- **사용자 GPS 좌표는 브라우저 밖으로 내보내지 않는다.**
  서버·DB·공유 링크 어디에도 담기지 않습니다.
  `tests/repository-policy.test.mjs`가 이 규칙들을 지킵니다.

---

## 화면을 만들 때

[`docs/design-system.md`](docs/design-system.md)를 따릅니다. 토큰을 먼저 찾아 쓰고,
없을 때만 새로 만들되 문서에도 함께 추가합니다.

특히 이 서비스는 "갈 수 있는지"를 확인하러 오는 사람이 씁니다.
보기 좋음보다 읽히는지가 먼저입니다.

- 본문·라벨 대비 4.5:1 이상. 큰 제목과 비활성 요소만 3:1까지.
- 사진 위에 글자를 얹으면 반드시 스크림을 깐다.
- 색만으로 상태를 말하지 않는다. 글자나 아이콘을 함께 쓴다.
- 조작 대상은 최소 44px.
- 애니메이션은 `prefers-reduced-motion: reduce`에서 멈춘다.

**어두운 구역을 밝게 바꿀 때는 안쪽 글자색도 함께 바꾼다.**
배경만 바꿔 흰 글자가 흰 배경 위에 남은 사고가 있었습니다.

---

## 환경

- Node.js `22.13.0` 이상
- `.env.local`과 실제 인증키는 절대 커밋하지 않습니다.
- 저장소 경로에 한글이 있으면 Vite가 모듈 URL을 인코딩하면서 React 인스턴스가
  갈라져 모든 페이지가 500으로 죽습니다. ASCII 경로에서 개발 서버를 띄웁니다.
