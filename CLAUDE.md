# WAVE Engineering Guide

이 문서는 구현·검증 절차를 다룬다. 권한·비용·출시 판단은 [AGENTS.md](AGENTS.md)를 따르며 같은 규칙을 별도로 재정의하지 않는다.

## 작업 절차

1. 현재 사용자 목표와 수용 기준, 기존 코드·실제 Production·관련 이슈를 확인한다. 지시된 범위 안에서 재현된 문제부터 처리한다.
2. 최신 `origin/main`과 현재 변경을 확인하고 독립 브랜치에서 작업한다. 사용자의 미커밋 변경을 덮어쓰지 않는다.
3. 하나의 사용자 목표에 필요한 코드·상태·문서·검증을 함께 완성한다. 파일이나 화면마다 인위적으로 PR을 나누어 대기와 CI를 반복하지 않는다. 독립적으로 되돌릴 수 있는 무관한 목표는 별도로 처리한다.
4. 구현 중에는 관련 단위·타입·브라우저 검사부터 실행한다. 최종 후보에서 아래 품질 검사와 변경된 핵심 흐름의 브라우저·접근성 검증을 마친다.
5. 결과를 PR과 [AI 작업 로그](docs/ai-logs/TEMPLATE.md)에 기록한다. PR 작성자를 assignee로, 기존 라벨 중 변경 성격에 맞는 라벨을 지정한다. 이미 적용 중인 Owner 리뷰 면제를 다시 묻지 않는다.
6. 병합 직전 최신 main 포함 여부와 필수 validate 성공을 확인한다. 이후 CD와 실제 Production 핵심 여정까지 확인한다. 완료 범위와 외부 제약을 사실대로 보고한다.

```sh
git fetch origin main
git merge-base --is-ancestor origin/main HEAD
npm run lint
npm run typecheck
npm test
npm run build:vercel
npm run check:performance
```

main이 바뀌면 충돌을 해결하고 변경 영향에 맞는 로컬 재검증을 수행한 뒤 갱신된 최종 후보의 CI를 확인한다. 내용이 달라진 이전 결과는 재사용하지 않는다. 인증된 동일 tree의 main CI 재사용만 [CI 계약](docs/ci-validation.md)에 따른다.

## 변경에 맞는 검증

- 여행 상태·저장·계정·AI 실행: 오래된 응답, 취소, 저장 실패, 로그인 만료, 원자적 적용과 복구를 검증한다.
- 화면·레이아웃: 실제 렌더링으로 1440px·960px·390px, 키보드 초점·44px 조작 영역·넘침·사진 비율을 확인한다. 디자인은 [design-system.md](docs/design-system.md)의 토큰을 사용한다.
- 데이터 연동: HTTP 성공만 보지 말고 실제 응답의 장소 ID·지역·기간·제공처 상태와 화면 반영을 확인한다. 합성 fixture와 실제 조회를 결과에 구별해 적는다.
- 모델: 대표 요청의 의도·구체적 일정·보존 조건·실패 복구를 측정한다. 모델 이름이나 GPU 수를 품질 증거로 대신하지 않는다.
- 작은 가역적 스타일 수정에 구현을 그대로 복제하는 테스트를 만들지 않는다. 새 기능의 중요한 실패 경계와 회귀는 테스트로 고정한다.
- 전체 검증을 생략해 속도를 얻지 않는다. 구현 중 중복 실행과 동일 콘텐츠에 대한 반복 검증을 줄인다.

## 유지할 기술 결정

- 계정은 `/login`, `/register`, 개인 여행 화면에서 제공하고 핵심 Planner를 막지 않는다. 비밀번호를 서비스 DB에 저장하지 않는다. 계정 복귀 경로는 허용된 same-origin 경로만 사용한다.
- 한국관광공사 원본 OpenAPI 응답을 서비스 DB에 복제하지 않는다. 현재 응답 캐시·재확인 정책은 [API 기록](docs/api-integration-audit.md)을 따른다. 캐시 결과를 새로운 실호출로 표시하지 않는다.
- 브라우저 GPS 좌표는 별도로 구분하여 WAVE 경로 API 요청을 차단한다. 공개 장소·직접 고른 지도 좌표는 경로 제공처에 전달될 수 있다. 지도 표시·주변 검색의 SDK 제공처 처리와 DB·공유 저장 경계를 개인정보 안내에 정확히 적는다.
- 나루는 신뢰할 수 없는 모델 출력을 허용 목록과 실제 장소 ID로 검증한다. 외부 문장·모델 문자열을 코드·임의 URL·DB 명령으로 실행하지 않는다. 채팅과 직접 편집의 여행 상태를 중복 구현하지 않는다.
- 본문·라벨 대비는 4.5:1, 큰 글자는 3:1 이상이다. 사진 위 글자에는 스크림, 상태에는 색 외 설명을 제공한다. 모션 감소 설정에서 불필요한 움직임을 멈춘다.
- Node.js 22.13 이상을 사용한다. 이 저장소의 Vite 개발 서버는 ASCII 경로에서 실행한다. `.env.local`, 키·토큰·비밀번호는 커밋하지 않는다.

## 개발 에이전트 지침 근거

[OpenAI 공식 GPT-6 Astra 안내](https://developers.openai.com/api/docs/guides/latest-model?model=gpt-6-astra)를 2026-09-12 확인했다. 목표·맥락·완료 기준을 명확히 하고, 필요한 도구로 근거를 확인하며, 권한 안에서 구현과 검증을 끝까지 수행하는 개발 지침으로 사용한다. 서비스의 나루 모델을 유료 Astra API로 바꾸는 지시가 아니다.

과거 정책·사건의 상세 이력은 Git 기록과 관련 이슈에 남긴다. 현재 실행 규칙을 변경하면 이 문서와 AGENTS의 중복·상충 문장도 함께 정리한다.
