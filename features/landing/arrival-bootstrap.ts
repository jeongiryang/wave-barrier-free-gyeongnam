/** Only the pre-hydration exit. Never starts media or replaces hydrated dialog controls. */
export const arrivalBootstrap = `(()=>{const exit=()=>{const n=Array.from(document.querySelectorAll('.arrival-intro[open]')).find(d=>d.getClientRects().length);if(!n||n.dataset.ready==='true')return;try{sessionStorage.setItem('wave-arrival-session-v1','done')}catch{}n.close();n.hidden=true;document.documentElement.dataset.introSeen='1';document.documentElement.dataset.introFocus='pending';document.getElementById('landing-title')?.focus({preventScroll:true})};document.addEventListener('keydown',e=>{if(e.key==='Escape'){exit()}},true);setTimeout(exit,5200)})()`;
