/** Only the pre-hydration exit. Never starts media or replaces hydrated dialog controls. */
export const arrivalBootstrap = `(()=>{const exit=e=>{let n;if(e.type==='keydown'){if(e.key!=='Escape')return;n=Array.from(document.querySelectorAll('.arrival-intro[open]')).find(d=>d.getClientRects().length)}else{const b=e.target instanceof Element?e.target.closest('[data-intro-skip]'):null;n=b?.closest('dialog')}if(!n||n.dataset.ready==='true')return;e.preventDefault();try{sessionStorage.setItem('wave-arrival-session-v1','done')}catch{}n.close();n.hidden=true;document.documentElement.dataset.introSeen='1';document.documentElement.dataset.introFocus='pending';document.getElementById('landing-title')?.focus({preventScroll:true})};document.addEventListener('click',exit,true);document.addEventListener('keydown',exit,true)})()`;
