/** Pre-hydration exit starts when the streamed intro arrives, never at head evaluation. */
export const arrivalBootstrap = `(()=>{const exit=()=>{const n=document.querySelector('.arrival-intro[open]');if(!n||n.dataset.ready==='true')return;try{sessionStorage.setItem('wave-arrival-session-v1','done')}catch{}n.close();n.hidden=true;document.documentElement.dataset.introSeen='1';document.documentElement.dataset.introFocus='pending';document.getElementById('landing-title')?.focus({preventScroll:true})};let timer;const arm=()=>{if(timer||!document.querySelector('.arrival-intro[open]'))return;timer=setTimeout(exit,5200);observer.disconnect()};const observer=new MutationObserver(arm);observer.observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('keydown',e=>{if(e.key==='Escape'){exit()}},true);document.addEventListener('DOMContentLoaded',()=>{arm();observer.disconnect()},{once:true});arm()})()`;
