import { regionBoundaries } from "../region-boundaries";
export default function LandingItineraryMap({en}:{en:boolean}) { return <div className="night-itinerary-map" aria-label={en?'Explore the places on the Gyeongnam map':'경남 지도에서 여행지 살펴보기'}><svg viewBox="0 0 800 814" role="img" aria-label={en?'Tongyeong, Geoje and Hadong in Gyeongnam':'경남 내 통영·거제·하동 위치'}>{regionBoundaries.map(area=><path key={area.name} d={area.path} fillRule="evenodd" data-featured={['통영','거제','하동'].includes(area.name)}/>)}{regionBoundaries.filter(area=>['통영','거제','하동'].includes(area.name)).map(area=><g key={area.name}><circle cx={area.x} cy={area.y} r="17"/><text x={area.x} y={area.y-28} textAnchor="middle">{area.name}</text></g>)}</svg><div><strong>{en?'A journey shaped around you':'장소에서 나만의 일정으로'}</strong><p>{en?'Choose your places, then check the route and timing.':'마음에 드는 장소를 담고 이동 경로와 시간을 확인하세요.'}</p></div></div>; }
