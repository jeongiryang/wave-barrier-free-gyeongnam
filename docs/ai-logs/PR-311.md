# PR 311 — 핵심 여정과 한국어 정보구조 정리

## 기준

- 기준 브랜치: `fix/production-journey-completion` (PR #307)
- 기준 SHA: `8b0257c891e7f624d2dbf0ac5d7f5b6f568091a0`
- 변경 성격: 저장·공유·인증·서버 API 계약을 바꾸지 않는 UI/UX 개편

## 제품 결정

- 랜딩의 주요 행동은 `여행 계획 만들기` 하나로 통일했다.
- 조건 입력은 `지역 → 필요한 편의 → 하고 싶은 활동 → 날짜` 네 질문으로 확정했다.
- `여행집`, `이 기기 일정`은 기본 메뉴에서 `내 일정`으로 통일하고, 저장 범위는 보조 문구 `이 기기에 저장돼요`로 정확히 알린다.
- Hero의 장식성 경로 모형, 포인터 조명, 챕터 레일과 데이터 원칙 중복 섹션은 기본 화면에서 제거했다. 공식 정보와 미확인 정보의 구분 원칙은 Hero 안내와 실제 결과 화면에서 유지한다.
- 경남 지역 선택을 기능 설명보다 앞에 두어 실제 콘텐츠와 시작 행동을 먼저 보이게 했다.

## 참고 디자인 적용

- 채택: 콘텐츠 우선, 한 화면 한 주요 행동, 타이포·여백 중심 위계, 색 외 번호/문구 상태 표시.
- 비채택: Apple 색상·SF Pro·36px 버튼 복제, 전면 유리 효과, 모든 카드의 그림자·모션.
- 브랜드: 기존 Deep Ocean 색상과 44px 조작 영역을 유지했다.

## 호환과 롤백

- 로컬 저장 키, 일정 스냅샷, 공유 URL, 인증 복귀 URL, 관광·경로·날씨 API를 변경하지 않았다.
- 롤백은 이 커밋만 되돌리면 되며 데이터 migration은 없다.

## 검증

- `npm run typecheck`: PASS
- `npm run lint`: PASS
- `npm test`: PASS (280/280)
- `npm run build:vercel`: PASS
- `npm run check:performance`: PASS
- `npm run test:e2e`: 로컬 Playwright 브라우저 실행 파일 부재로 시작 불가. 원격 CI에서 재실행한다.

## 2026-09-06 통합 회귀 감사

- 실제 PR은 #311이다. 기존 PR-308.md 파일의 잘못된 번호를 정정했으며 이전 Git 기록은 보존한다.
- #287/#307/#311, #309, #289~#306을 격리 worktree에서 합성했다. CI 파일의 actionlint와 전체 audit을 함께 유지했다.
- #311 HEAD ec1fe826의 CI 34019899171은 cancelled다. 이를 성공으로 기록하지 않는다.
- 날짜 질문 단계와 변경된 검색·일정·저장 CTA를 화면, error-context, trace로 대조했다. E2E setup이 마지막 날짜 단계를 거치고 명시적으로 검색하도록 수정했다.
- 의미가 유지된 접근 가능한 이름을 현재 UI와 일치시켰다. 제거된 signal-node 대신 새 네 단계 안내의 제목·번호·설명을 모두 4.5:1로 검사한다. 터치 검사는 실제 헤더 CTA를 포함하며 44px/25개 하한을 유지한다.
- timeout 증가·새 skip·assertion 제거 없음. 전체 구간·날짜·준비율·키보드·저장/복원 검증을 유지했다.
- 합성 후보 unit/contract 306/306, lint, typecheck, audit 0, Vercel build, performance PASS.
- 첫 전체 Playwright: 222 pass / 17 fail / 기존 skip 1. 수정 후 관련 7개 spec: 71 pass / 기존 skip 1. 전체 최종 회귀 및 새 HEAD CI는 진행 중이다.
- Production 34e6021 유지. 필수 승인 0/3, Preview·Production 수정본 검증은 아직 없다.