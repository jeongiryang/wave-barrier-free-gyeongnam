# Plan request failure guidance — #282

Codex is implementing the Owner's functional completion request. The current bounded change distinguishes timeout, HTTP5xx and offline failure from a generic failure in both guided conditions and overview results. It retains current choices, prior results and saved places; previous results cannot gain new saved places until a successful retry. Raw server messages are not shown as localized guidance. Parent abort remains handled before classification and is not a user failure.

Baseline fa30f69 reproduces two real desktop failures: the actual client timeout and HTTP503 response share the generic alert rather than identifying the cause. Initial four English failures were test setup: the old-result action was in a hidden guided stage. A second attempt used an auto-hidden header link and timed out outside the viewport. Corrected tests navigate through the existing persistent step rail. These setup failures are not product fixes.

Current12 browser cases PASS23.7s: timeout/server/offline × Korean overview light/English guided dark × desktop/mobile. Assert specific alert, saved1001, disabled new-place action on previous results, available remove action, accessible results, exact one manual retry, and restored availability after success. Initial full typecheck needed an explicit literal return union. An existing hook test mocked the entire API module and lacked the new classifier; it now imports the actual compiled API exports while retaining only the controlled transport response. Full quality and existing regressions will be rerun before a PR.

The offline fixture exposed a separate real failure: taking the browser offline while TravelBookArchiveAction was still loading rejected its uncaught lazy import and entered whole-page recovery. The screenshot and error context are preserved in wave-plan-failure-final-output. The classification test now explicitly waits for the archive control before taking the browser offline; this isolates a previously loaded app's API failure and does not claim the module failure was repaired. That module boundary remains a separate follow-up. New malformed-success-plan validation is also unverified; code currently assumes response.statuses after storing the response.

No backend/provider requests, cache, retry count, timeout, account operation, dependency or saved-data format is changed. No PR or Production claim yet. Full #282 stale-data/partial-source preservation remains open.

## Main #422 validation
Normal integration of main a23afdd1156008ceb45bedcaffedc5a09460c610 retains the connection repair. Lint/typecheck,748 unit/contract tests and Vercel build PASS. All70 browsers PASS2.1min (new failure12,core18,search-result-focus40). Actual960 KOlight/1440 ENdark timeout captures were visually reviewed: cause and retry are readable, prior cards remain visible. Performance FAIL: Planner270.08/270KiB; no budget relaxation. The pending #420 summary extraction is expected to reduce initial loading, but integration and fresh measurements are still required before a PR. No new error-specific lazy module was added.

## Main #420 verification
Normal integration of d21258c569b50afd6acafa58978e22734ae25dad preserves the arrival summary extraction. Fresh lint/typecheck, all749 unit/contract tests, Vercel build and unchanged performance budgets PASS (Planner269.74/270KiB, CSS69.90/70KiB). All70 browser cases PASS2.1min. The previous main #422 size failure is resolved by integration; no budget or assertion changed. The separate archive module recovery is now PR #423; this branch will integrate its actual main and run fresh checks before its own PR.
