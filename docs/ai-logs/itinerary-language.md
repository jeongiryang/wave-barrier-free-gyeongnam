# 날짜별 일정의 한국어·영어와 선택 사항 접근성 — 2026-09-06

## 범위와 근거

#322 `0b916a4` 기반의 `fix/itinerary-language`다. 공유 스냅샷 수정은 그대로 포함하고 별도 PR로 유지한다.
이전 후보 `c904ec0`에 같은 브라우저 검사를 실행해 영어 일정 region/조작을 찾지 못하는 6건 실패를 확인했다.
기존 한국어 계약·장소 ID·여행 날짜·방문 순서·저장 형식·공유 본문은 변경하지 않는다.

## 수정

- 일정 제목·날짜 범위·시간·순서·이동 추정의 한계·날짜 밖 장소·공유·기기 보관·선택 사항을 KO/EN으로 표시한다.
- 원문 장소명/주소/설명/오디오 대본에는 원문 언어를 표시한다. 공식 정보 확인률과 직선거리 기반 순서를
  실제 이동시간으로 설명하지 않는다. 날짜 헤더는 언어에 맞춰 표시하지만 저장값은 ISO 날짜다.
- 편집/정렬/저장 안내는 번역된 문자열을 상태에 저장하지 않는다. 언어 변경 후 현재 안내도 갱신된다.
- 오디오가 없으면 재생/탐색을 비활성화하고 ‘준비 중’ 대신 미제공 상태를 알린다. 재생 거부를 처리해
  unhandled rejection을 막고 원문 대본·재시도 안내를 제공한다. 닫으면 재생 상태를 정리한다.
- 빈 일정에서는 편집기, 닫힌 선택 사항에서는 오디오 모듈을 요청하지 않는다. 모듈 실패 시 설명을 제공하며
  추천/일정의 다른 조작을 유지한다. 초기 JS 270.55 → 270.22 → **265.39/270 KiB**로 예산을 지켰다.
- 밝은 화면에서 어두운 전용 오디오 스타일을 사용하던 대비와 dark 시간 입력의 흰 배경을 수정했다.
- 첫 320px 캡처에서 DAY 2 잘림과 Earlier 줄바꿈을 발견했다. 중첩 grid의 암시적 열을 없애고
  400px 이하에서 순서 조작을 한 열로 배치했다. 레이블 실측 검사를 추가하고 수정 후 화면을 직접 확인했다.

## 검증

- lint/typecheck, 전체 unit·contract **280/280 PASS**.
- 관련 일정·공유·인트로 회귀 **72/72 PASS**.
- 최종 새 브라우저/axe **12/12 PASS**: 날짜·순서·복원·언어 전환, 원문 표시,
  미제공/재생 거부/모듈 실패, 실제 지연 로딩, 320/960/1366px × light/dark.
- 편집·저장 12개 고유 화면(프로젝트 중복 포함 24캡처), 초기 오디오 4캡처. 터치 44px·가로 넘침·라벨 폭·axe 확인.
  960 dark·320 light와 수정 전후 320 화면을 직접 검수했다. 브라우저 자체 시간 입력의 오전/오후는 OS/브라우저 언어를 따른다.
- 최종 Vercel build/performance PASS: CSS gzip **69.22/70 KiB**, planner JS **265.39/270 KiB**.
- 첫 전체 실행은 캡처에서 발견한 잘림을 수정하기 위해 중단했다. 성공으로 세지 않는다.
  최종 전체 Playwright·axe는 실행 중이며 실제 완료 결과와 CI를 후속 기록한다.

신규 skip·timeout 증가·assertion 완화 없음. mock API/브라우저 media 거부 검사는 실제 관광 API나 오디오 운영 검증이 아니다.
현재 main/Production은 34e6021이며 본 변경은 미배포다. Refs #251 #264 #265 #281 #284 #285 #286.
인증 폼·정책 본문·경로 상세의 영어, 200% 확대·실기기/화면 낭독기·Preview/008/운영 API는 남는다.
모델 API·Secret·새 유료 서비스·예약 생성 없음. Release GO를 선언하지 않는다.
