# RC334 preferences focus visibility — 2026-09-08

Refs #285, #334. Actual authenticated ffa4603 Preview at320px English/dark/reduced-motion reproduced a P1 keyboard visibility defect: open Preferences, Tab through Language/Appearance/Motion to Log in and then Step by step. The fixed preference panel stayed open and covered the focused body control. Escape also left it open. No forced click or injected app state was used to reproduce it.

`PreferenceControls` now closes its native disclosure when focus leaves its subtree without moving that focus. An explicit Escape closes immediately and focuses its own summary; it does not schedule a delayed focus restoration. Theme changes, repeated intro replay and runtime reduced-motion changes keep the existing controls mounted and focused.

New browser regression runs KO/EN at320/1366 in both desktop/mobile projects, naturalTab leaving, Escape, dark appearance, runtime reduced-motion, support-language notice, axe, console errors and overflow. Before the product fix, targeted KO320 fails in both projects at the still-open disclosure assertion, including the unchanged configured retry. Preserved trace/screenshot/error-context: wave-resume-830-artifacts/preferences-before-results. After the fix, all8 new cases plus2 existing intro replay cases pass,0fail/skip/flaky (12.4s; final product12.6s). Existing intro assertions were not edited.

The first product build exceeded the unchanged planner270KiB budget at270.07KiB. Remove unreachable KO-partial/EN-full preference branches under the existing two-language contract, reuse the existing English flag, and keep the actual displayed support notices. The final Windows build measures269.97/270KiB and CSS69.95/70KiB. The two existing repository source-shape assertions now require the same hydration guards plusonBlur, and the actual KO-full/EN-partial notices; corresponding visible notices are also required by browser tests. No test was removed, skipped, timed out more generously or changed to force an interaction.

Local lint0errors/2existingwarnings, typecheck, unit/contract629PASS, build and budgetsPASS. Production/full audits0 from the same unchanged dependency lockfile. New hosted complete CI/Preview/independent QA remain required after pushing this fix. CI832 for predecessorffa4603 is a separate historical result, not evidence for this commit. No merge, migration, Production write or automation activation is claimed.
