# RC334 Cron credential input integrity — 2026-09-08

Refs #334, #11, #288. Owner authorization comment5578649634 permits direct secret-safe DB operation verification and release continuation.

The official Vercel CLI was authenticated through the Owner's existing browser session. Production sensitive variables are correctly not exported by `vercel env run`; no connection value was printed or copied. An exact f9bac18 archive was therefore deployed with `--prod --skip-domain` to run the reviewed authenticated READ ONLY inspect handler inside the actual Production environment.

Vercel rejected candidate dpl_H1us6fdL6RR2iSk8AATcSrUzNtJp with `INVALID_CRON_SECRET`: the existing project Production variable has leading or trailing whitespace. The active Production deployment remained dpl_DpUP8LhruYLeXs6N5Nxqrk3JoD6G / 34e6021. No DB migration or domain promotion occurred. This is an actual deployment configuration defect, not evidence of a product test failure or a DB target mismatch.

The CD creation command appended a newline to the sensitive variable's stdin. It now writes exactly the generated bytes. Production environment validation also rejects surrounding whitespace instead of silently accepting a trimmed value; regression assertions cover spaces, LF and CRLF and prove errors do not contain the value. The stored secret's original creation source is not asserted: its value is intentionally unreadable. Cron's project-level single-secret contract remains unchanged; no per-deployment Cron override or policy weakening is introduced.

Validation and the actual operational repair are recorded in the existing #288 / #334 evidence ledger. The failed candidate, old CI/Preview and all original worktrees/user changes remain preserved. This change does not claim production deployment, DB binding success or automation activation.

Local checks: related deployment/preflight/retention tests 18 PASS, full unit/contract 629 PASS (0 failure/skip), lint 0 errors/2 existing warnings, typecheck PASS, Vercel build PASS, performance budget PASS (CSS69.95/70KiB; planner269.97/270KiB). No assertions were removed, no skip was added, and no timeout or budget was changed. Full hosted CI and independent review must run on the new commit.

At 2026-09-08T03:43:35Z, the existing Production-only sensitive CRON_SECRET was repaired using the authenticated official CLI, supplying 64 random hexadecimal characters through stdin without a newline. Its value was neither printed nor persisted locally. Project-level scope and sensitivity were preserved; DATABASE_URL metadata remained unchanged. No provider key, auth key, domain, ruleset or DB data was modified. The current deployment was not promoted or changed, and the retention endpoint was not invoked. The daily03:17UTC schedule had already passed; the next production deployment must use the same project-level value for scheduler and handler.
