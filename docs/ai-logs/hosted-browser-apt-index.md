# Hosted Chromium installation: unrelated Google APT index failure

- ISSUE: Production QA after #400 could not start its read-only browser checks. The Owner explicitly prioritized failed post-merge deployment validation and authorized CI reliability repairs.
- REPRODUCTION: Run `34383306891`, attempts 1 and 2, both fail `npx playwright install --with-deps chromium` with APT exit 100. Google's `chrome-stable/deb` package index hash differs from its signed Release metadata. Ubuntu package sources succeed. A single bounded rerun confirmed persistence; no retry loop was added.
- ROOT CAUSE: Playwright's required Ubuntu dependency installation updates every configured APT repository, including an unrelated Google Chrome repository supplied by the ephemeral runner.
- CHANGE: Before active browser installations and the existing sandbox dependency installation, quarantine only the fixed Google Chrome source filenames when they contain Google's Chrome APT URI. Files move to `RUNNER_TEMP`; installed Chrome and Ubuntu sources remain available. The official runner image already removes the older Chrome source filename during image creation. Both current `.list`/`.sources` and `google-chrome`/`google-chrome-stable` names are covered.
- PRESERVED: The pinned Playwright version and `--with-deps chromium`, all eight browser shards, assertions, retries, fail-on-flaky, timeouts, performance budgets, required `validate`, package authentication/hash checks, immutable bootstrap and complete sandbox commands/probes. No archived workflow was enabled. No application source, dependencies, lockfile or deployment routing changed.
- VALIDATION: Fresh local lint PASS (0 errors, 13 existing warnings), typecheck PASS, all 708 unit/contract tests PASS, Vercel build and unchanged performance budgets PASS. Actionlint 1.7.12 PASS after verifying its release asset SHA-256. The regression contract still compares every frozen sandbox command/probe against the archived reference, permitting only the separate runner preparation step after bootstrap verification. Actual hosted installation/browser results and post-merge Production validation remain pending; no hosted success is inferred from local YAML checks.
- SEPARATE HOLD: #372 still blocks live ODsay calls. Read-only browser checks continue independently; this repair does not clear that hold or claim full Release GO.

References: [failing run](https://github.com/jeongiryang/wave-barrier-free-gyeongnam/actions/runs/34383306891/attempts/2), [runner image source](https://github.com/actions/runner-images/blob/ubuntu24/20260831.293/images/ubuntu/scripts/build/install-google-chrome.sh), [Playwright CI installation](https://playwright.dev/docs/ci).
