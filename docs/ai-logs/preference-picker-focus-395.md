# Language label crash — #395

- REPRODUCTION: Clicking `한국어 전체 지원` crashes Chromium on the current Production and local Planner. New repository browser tests reproduce `locator.click: Target crashed` in all four Planner/Community × desktop/mobile cases before the fix. Evidence: `%TEMP%/wave-preference-before.log` and its traces.
- ROOT CAUSE: A label click temporarily moves focus to null before forwarding activation to its native select. The disclosure's unconditional blur handler hides that select's parent during picker activation.
- CHANGE: Preserve the disclosure during null-focus transitions. Keep existing closure when keyboard focus moves outside, add document pointerdown dismissal for genuine outside clicks, and clean up the listener on unmount. Native picker, hydration guards, language choices and Escape/focus return remain intact.
- BROWSER: All 12 focused crash/focus/keyboard/axe cases PASS. The four previously crashing cases now change language with keyboard input and close both native picker and disclosure. Real CUA label click also stays alive and switches language; restored Korean afterward. Planner and Community 960px/1440px menu captures visually inspected.
- FAILED CHECKS: First post-fix test expected the whole document to become English, contrary to the existing partial-translation language policy; the exact selected language and visible translated copy are asserted instead while `html.lang=ko` remains protected. The next run established that Escape first dismisses the native picker and then the disclosure; both states are now explicitly checked. Full unit suite also identified a source-pattern assertion that needed the new disclosure ref while retaining all hydration guard assertions.
- QUALITY: Fresh lint (0 errors, 13 existing warnings), typecheck, all 708 unit tests, build:vercel and unchanged performance budgets PASS after the source-pattern adjustment. Current main CI independently hit a virtual-clock timing race; repair #403 takes priority before this PR proceeds. Integration with the subsequently deployed main and hosted validation remain pending.
- LIMITATION: This bounded fix does not close all #395 reports or claim full Production validation. No CSS, translation policy, native picker implementation, dependencies or lockfile changes.
- LATEST MAIN: After #403 deployed and #389 merged, integrated `b275246db147e6e13647e7673eae3a3e3b6f399b` normally at `46bb793658beb2826b1b54dd2da710804421e29c`. Fresh lint (13 existing warnings), typecheck, 722 unit tests, build:vercel and unchanged performance budgets PASS; all 12 focused browser cases PASS again (10.6s). Evidence: `%TEMP%/wave-preference-main389-*`. Hosted CI and exact-SHA Production remain pending.
