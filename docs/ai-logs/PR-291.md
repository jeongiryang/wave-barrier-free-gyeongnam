# PR #291 AI 작업 로그

- PR: https://github.com/jeongiryang/wave-barrier-free-gyeongnam/pull/291
- 제목: ci: workflow 실패를 GitHub Issue로 자동 환류한다
- 작성자: jeongiryang
- 최종 상태: OPEN
- AI 도구: ChatGPT / GitHub connector

## 목적

CI, CD, Production API Smoke 실패를 사람이 Actions에서 발견해 다른 AI에게 전달하지 않아도 GitHub Issue 작업 큐로 자동 환류하는 W.A.V.E 자동화 Control Plane의 첫 실행형 loop를 추가한다.

## 역할 구분

- 사람(PM, `jeongiryang`): 제품/Release 정책과 최종 human gate
- AI: 현재 workflow 구조 조사, failure routing 설계, GitHub Issue #290 및 workflow 구현, PR 작성과 검증 상태 추적

## 검증

- 최신 `main` `34e6021265b16d046dca24feaa3ec2101fc977e2`에서 분기
- 감시 대상 workflow 이름이 현재 저장소의 `CI`, `CD`, `Production API Smoke`와 일치함을 확인
- 신규 `.github/workflows/automation-failure-router.yml`만 추가하여 PR #287이 수정 중인 기존 workflow 파일과 직접 충돌하지 않음
- `workflow_run` 완료 이벤트 중 `conclusion != success`만 처리
- workflow별 marker + title로 열린 기존 failure Issue를 찾아 반복 실패는 comment로 누적
- 신규 failure Issue에는 `bug` 라벨과 `jeongiryang` assignee를 지정
- Secret/전체 로그 원문은 Issue에 복제하지 않음
- router 자체는 감시 대상 목록에 없어 자기 루프가 없음
- 저장소 CI 결과를 PR에서 확인

## 결과와 제한

- merge 후부터 대상 workflow 비성공 상태가 GitHub Issue로 자동 환류됨
- 성공 재실행만으로 Issue를 자동 close하지 않음. 실제 원인 해결과 검증은 PM/Engineering/QA loop에서 처리해야 함
- Production/CI를 고의로 실패시키지 않으며 실제 failure routing 검증은 안전한 후속 fixture 또는 자연 발생 실패에서 확인함
- Codex 자동 실행과 PM 자동 triage는 #288 후속 Phase임
