# Selected travel mode provider scope — #387

Owner authorized functional completion, tested merges and Production verification. This is a bounded follow-up; no complete #387 or Release GO claim.

Baseline182e762 on actual main #424 reproduces five failures: car/transit/walk/bicycle all invoke Kakao+ODsay+public-context, and an invalid mode is accepted. The current Planner now sends the displayed mode for the selected leg and whole-itinerary checks. It starts with the existing pre-result Public transport choice and does not silently change it to the fastest returned mode. Mode controls explicitly reload the currently displayed public leg, preserving its start and destination. GPS privacy, cancellation, two-worker itinerary budget, server/client timeouts and storage remain unchanged.

The server limits car to Kakao, transit to public context/ODsay, and unsupported walk/bicycle to no route-provider calls with an unavailable response. Invalid modes fail before providers. The omitted-mode legacy comparison contract remains compatible with older clients and the existing Production smoke; current Planner always sends mode. No approved walking/bicycle provider, paid activation or live #372 probe is claimed. Existing provider access/fare/geometry validation is retained.

Whole-itinerary evidence includes mode in its identity. Completed records may be reused for the exact same mode/date/order/origin identity; pending obsolete responses cannot overwrite the current view. This is not a new freshness/TTL policy. The existing completed-car → walk → completed-car regression remains intact, with an explicit car selection because its fixture is car-only.

Initial full lint/typecheck,755 unit/contract tests, Vercel build and performance PASS on #424 (Planner269.80/270KiB,CSS69.90/70KiB;13 existing warnings). The new six provider-scope/legacy contracts pass. New four browser mode-request/pending-cancellation cases, two itinerary-sync cases and eight existing departure-route cases PASS14 total23.0s, including axe. An earlier assertion used the wrong museum longitude and was corrected to the canonical fixture value. An initial full-page axe check ran during a reveal transition; the final test uses the same reduced-motion setup as the existing accessibility suite and retains every axe rule. Two legacy API test loaders were extended for the new provider-data boundary; all their original assertions remain. An intermediate broad run failed because the car-only scenario had not explicitly selected car; an additional zero-after-switch-back expectation was removed to preserve the existing same-identity completed evidence contract.

Broader browser regression, fresh actual main integration, final rendering review, hosted CI, PR and deployment remain pending. Do not merge this local candidate yet.