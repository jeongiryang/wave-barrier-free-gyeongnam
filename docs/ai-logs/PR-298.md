> 2026-09-05 Engineering 감사 보충: 현재 PM은 사용자가 관리하는 ChatGPT Work PM 채팅이다.
> [통합 감사](../automation-integration-audit.md)와 [구독 전용 계약](../subscription-only-automation.md)이 아래 과거 API 활성화 안내보다 우선한다.
> 기존 구현을 보존하고 역할·YAML·권한·이벤트·중복·Production read-only 경계를 보강했다.
> #296/#298/#300 API workflow는 GitHub disabled_manually 및 코드 상수 false이며 #302 API notify도 차단했다.
> 로컬 구독 고정 fixture는 PASS이나 Scheduled/실제 queue/독립 AI QA의 종단간 동작은 미검증이다.
> 병합/활성화/완료는 서로 구분하며 실제 AC가 남은 Issue는 닫지 않는다. 정확한 HEAD CI는 PR 보충 댓글을 따른다.

# PR #298 AI 작업 로그

- PR: https://github.com/jeongiryang/wave-barrier-free-gyeongnam/pull/298
- 제목: ci: trusted PM handoff 기반 Codex Engineering Worker를 추가한다
- 작성자: jeongiryang
- 최종 상태: OPEN
- AI 도구: ChatGPT / GitHub connector / OpenAI Codex GitHub Action 설계

## 목적

PM Agent가 승인한 `ready-for-dev` Issue를 사람이 VS Code Codex에 복사하지 않아도 Codex가 구현하고, 생성 권한·검증 권한·GitHub publish 권한을 서로 격리한 상태로 검증된 patch만 PR까지 자동 게시하도록 한다.

## 권한 구조

- 정이량: Repository Owner / Product Owner / PM / Release Authority
- PM Agent: `jeongiryang` identity의 trusted `wave-pm-task:v1` handoff 작성
- Codex: 승인된 engineering scope 구현만 담당
- Validate job: OpenAI/GitHub publish secret 없이 전체 회귀 검사
- Publish job: 검증된 patch만 branch/PR로 게시하며 generated code를 실행하지 않음
- QA/review: PR 이후 독립 검증

## 주요 보안 결정

- raw Issue body를 Codex prompt authority로 직접 사용하지 않음
- `status:ready-for-dev` label actor가 `jeongiryang`이어야 실행
- 최신 `jeongiryang` 작성 PM handoff comment의 `DECISION: READY_FOR_DEV` 필요
- Codex job에는 GitHub write credential 없음
- `sandbox: workspace-write`, `safety-strategy: drop-sudo`, trusted user 제한
- 민감 정책/자동화 경로는 handoff의 `ALLOW_SENSITIVE_AUTOMATION_CHANGES: true` 없으면 patch 단계에서 거부
- validation job에는 OpenAI key와 publish token 없음
- publish job에는 `WAVE_GITHUB_AUTOMATION_TOKEN`만 주고 앱/테스트를 실행하지 않음
- PR 게시 전 audit/lint/typecheck/unit/E2E/build/performance를 모두 통과해야 함

## 실패 환류

PR이 게시되지 않으면 원 Issue에 Automation recovery comment와 상태를 남기고, PM Workspace Agent가 설정되어 있으면 같은 Issue conversation으로 직접 recovery trigger를 보낸다.

## 외부 전제

- #294: `OPENAI_API_KEY`, `WAVE_GITHUB_AUTOMATION_TOKEN` 설정
- #293: Issue lifecycle labels 활성화
- #296: PM trusted handoff 생성 규칙 활성화
- 실제 smoke는 production issue가 아닌 안전한 automation fixture Issue로 수행

## 검증

- 공식 OpenAI `openai/codex-action@v1`의 sandbox, safety strategy, trusted trigger, prompt sanitation 지침 반영
- 일반 `GITHUB_TOKEN`로 생성한 PR의 후속 Actions 제약을 피하도록 publish credential 분리
- 이 PR 자체는 저장소 CI에서 YAML/정책 회귀를 검증함
