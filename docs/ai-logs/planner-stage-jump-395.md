# Planner stage navigation — #395 item 09

- Owner: reported the overview rail showing Places after navigating from departure to Itinerary, and abrupt nonadjacent jumps. Codex reproduced and repaired the bounded navigation behavior.
- REPRODUCTION: The expanded desktop/mobile, reduced/normal motion, 960/1440 flow failed all four overview variants; the screenshot shows the Itinerary heading and focused destination while Places remains highlighted. Guided variants passed. Initial mobile-only locator failures were a test assumption about hidden helper text and are not counted as product failures. Evidence: `%TEMP%/wave-stage-jump-audit.log` and output.
- ROOT CAUSE: IntersectionObserver callback entries contain changes, not the complete visible section set. A prior section crossing a threshold replaced a still-visible destination. Separately, the focus layout handler forced instant scrolling before the normal smooth request.
- CHANGE: Retain the currently intersecting section set and compare their current geometry when selecting the active rail item. Respect the existing reduced-motion helper when scrolling the focused target. No progress-completion, stored itinerary, provider, layout/CSS or access policy changes.
- VERIFICATION: All eight stage variants pass after the observer fix. Two normal-motion cases then reproduce the forced-auto-scroll contract; after the motion repair, all 14 related stage/control-center browser cases pass (35.1s), including native scroll behavior, focus, matching hash/rail/body, mobile targets and axe. Native scrollIntoView is instrumented but still executed, not stubbed. 960/1440 final captures inspected.
- QUALITY: All 733 unit tests, lint (13 existing warnings), typecheck/build/unchanged budgets passed after the observer repair. Final source on main #406 also passed all 733 unit tests, lint/typecheck/build/budgets and all 18 stage/control-center/restored-export browser cases (56.5s). Hosted CI, merge and exact Production remain pending. No full Release GO or live provider availability is claimed.
- REFERENCE: https://developer.mozilla.org/en-US/docs/Web/API/Intersection_Observer_API#intersection_change_callbacks

- MAIN #407 INTEGRATION: normal merge bcab49c; final 733 unit tests, lint/typecheck/build/performance budgets PASS (Planner 269.60/270 KiB, CSS 69.82/70 KiB). All 20 stage/control-center/crowd browser cases PASS (43.1s); final 960/1440 renders inspected. No assertion/retry/timeout/budget relaxation.
