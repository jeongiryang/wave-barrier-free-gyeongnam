# Neutral facilities and activities for a new trip — #388

- REPRODUCTION: In both desktop/mobile tests, choose facilities/activity, add a place and archive the itinerary, then select Goseong and explicitly start a new trip. The current itinerary empties but an old facility remains selected. Evidence: `%TEMP%/wave-new-trip-neutral-before.log` and traces.
- ROOT CAUSE: The existing fresh-trip callback resets routes and current places but not current criteria. An initial repair then exposed that the UI theme setter routed an empty value through the legacy API parser, silently selecting `nature`; both post-fix browser cases still failed. This failed evidence is retained in `wave-new-trip-neutral-fixed.log`.
- CHANGE: Only after the existing atomic new-trip storage replacement succeeds, clear current facilities and activities alongside existing origin/route resets. The UI setter preserves an intentionally empty theme value. Legacy API defaults remain unchanged. Adding a region keeps current selections; explicit saved-profile reuse and archived trips remain untouched.
- VALIDATION: All 16 browser cases PASS, including the two reproductions, delayed GPS responses, add/cancel/history, light/dark Korean/English dialog accessibility, dates/order reset and archive restoration. Neutral criteria, selected new region, native focus return and byte-identical archive are checked. 960/1440 px conditions were visually inspected. Fresh lint (13 existing warnings), typecheck, all 722 unit tests, build:vercel and unchanged performance budgets PASS.
- LIMITS: The separate travel-book `New trip` link, criteria restoration and other #388/#395 reports remain open. Latest-main integration, hosted CI and Production validation are pending after the preceding functional PRs. No archive deletion, profile consent change, API default change or automatic search.
- EVIDENCE: `%TEMP%/wave-new-trip-neutral-final.log`, `wave-new-trip-neutral-final-output` and `wave-new-trip-neutral-quality-*.log`.

- MAIN #407 INTEGRATION: normal merge bcab49c preserved the branch checkpoint d84c945. All 733 unit tests, lint (13 existing warnings), typecheck/build/performance budgets PASS (Planner 269.57/270 KiB, CSS 69.82/70 KiB). Six neutral/restored-export browser cases pass (23.2s), plus all 14 region-change boundary cases (1.0m). The initial command used a nonmatching boundary filename, so that suite was explicitly located and run separately; no 20-test claim was made from the first run. Final 960/1440 neutral criteria captures inspected. Hosted CI and exact Production remain pending.

- MAIN #408 INTEGRATION: normal merge51b06c, fresh733 unit tests/lint/typecheck/build/budgets PASS (Planner269.60/270 KiB, CSS69.82/70 KiB). All24 neutral, region-boundary and native stage navigation browser cases PASS1.5m. Final1440px rendering reviewed; 960px neutral layout was unchanged from verified main407. Hosted CI/Production pending.
