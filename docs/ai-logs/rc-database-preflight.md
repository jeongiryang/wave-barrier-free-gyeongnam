# RC334 Production binding preflight — 2026-09-08

Refs #334, #11, #288. Owner authorised direct secret-safe binding verification and safe deployment work.

The exact37a2421 RC received independent QA PASS5136267721 after full CI830 and authenticated Preview evidence; all8 prior P1 threads are resolved. The DB operation gate remained separate. Vercel connector get_project returned403, while authenticated Settings showed DATABASE_URL Production-only, unchanged sinceAug18, and explicitly write-only after saving. Edit was cancelled; no value was copied, printed, rotated or replaced. Metadata alone cannot establish the runtime endpoint.

The existing token-protected migration handler now supports an inspect mode before any migration statements. It matches the reviewed Neon endpoint/database, runs a PostgreSQL READ ONLY repeatable-read aggregate transaction with10s statement/2s lock limits, and returns only non-sensitive target IDs/counts. Wrong target/schema/read-only mode, malformed journals, provider errors and inconsistent counts stop progression. Apply also checks the same preflight before the unchanged canonical atomic migration chain. This is an operation gate, not new sandbox research.

Manual CD defaults to preflight-only, is Owner-only, and requires successful exact-SHA CI before executing candidate code with deployment credentials. It deploys with --prod --skip-domain and explicit SHA metadata. Inspection runs without migration, promotion, rollback or Cron configuration changes; a whitelisted artifact records the result. Normal main CI-triggered CD retains its complete release flow with the added read-only check. No model/API automation path is enabled.

Local validation of this change: related14 PASS, full unit/contract629 PASS (0fail/skip), lint0errors/2pre-existingwarnings, typecheckPASS, production/full audits0, Vercel buildPASS, unchanged budgetsPASS (Windows CSS69.95/70KiB; planner269.99/270KiB). A new workflow test initially matched even the safe renderer pipe as a raw response redirection; its pattern was corrected to distinguish the pipe, with CLI hostile-response tests retained. Existing tests/assertions/timeouts/workers/skip policy were not changed.

Hosted full CI, new exact-HEAD Preview, independent QA and actual protected runtime preflight are pending this commit. Original production DB remains unchanged; isolated PITR branch br-crimson-surf-aza5prlm and 8/4->8/1->8/4 rollback drill evidence remain preserved in #11 comment5577407732. Fresh deployment-time backup and impact verification remain required; no merge/Production success is claimed here.

Official contracts checked: [Vercel unpromoted Production deploy](https://vercel.com/docs/cli/deploying-from-cli), [Neon serverless READ ONLY transaction](https://github.com/neondatabase/serverless/blob/main/CONFIG.md), installed serverless1.1.0 type definitions. The existing included projects/resources are reused; no new paid service, model/API execution or subscription credential copy.

CI831 at40a40a3 found ShellCheck SC2016 on literal Markdown backticks in the shell summary format; remove only that decoration, leaving workflow checks strict. Log is preserved in wave-resume-830-artifacts/ci831-quality.log. Authenticated GitHub environment metadata additionally proves Production credentials are environment-only and its deployment branch policy permits main alone. No policy was changed and no blocked RC dispatch was attempted. Runtime preflight can execute after authorised main integration; until then the original binding cannot be read from the write-only Vercel Secret. Owner was asked only for the non-secret endpoint/database match, never the connection string. Other RC verification continues.
