# Recover an open tab after deployment — #395

- Actual canonical Production reproduction, 2026-09-09T21:33Z: a Planner tab opened on #412 navigated to the travel book after #414 deployed. It displayed CONNECTION PAUSE. The retry button repeated the failure. Browser console recorded `Failed to fetch dynamically imported module` for `/assets/page-BDbpmSoW.js`; a read-only request confirmed404. A full browser reload recovered the travel book. Subsequent actual #414 New Trip action reached `/planner#conditions` with neutral conditions and no saved places. [Issue receipt](https://github.com/jeongiryang/wave-barrier-free-gyeongnam/issues/395#issuecomment-5609065316).
- React boundary reset reused the current document's failed module import. The recovery button now explicitly reloads the document; the alternate Planner link also uses document navigation. No automatic reload loop, storage clearing, provider calls or deployment-setting changes. This fixes recovery once deployed; an already loaded older error boundary still needs the browser's Reload control once.
- Use the selected Korean/English language, identify the main landmark and keep an alert on the explanation. Local axe found the prior `role=alert` directly on `main` invalid; fixed the semantic structure without changing the visual layout. Copy only promises preservation of data already saved on this device, not unsaved text.
- Baseline test checkpointafa4fea. Initial hidden-header navigation was corrected to the actual saved-itinerary link. Two baseline tests then reached the real failed module and exposed the existing ARIA error. The actual Production retry failure above is separate evidence from that local baseline.
- Fresh main414 validation: lint13 existing warnings/no errors, typecheck, all736 unit/contract tests, Vercel build and unchanged performance budgets PASS (Planner269.80KiB, CSS69.90KiB). All14 targeted browsers PASS22.1s: eight module-failure/reload or Planner navigation cases across KOlight/ENdark and desktop/mobile, plus six new-trip preservation cases. Exactly one document navigation, current-trip/archive byte preservation, keyboard, locale, axe and overflow are checked. Rendered960KO and1440EN captures reviewed.
- This deployment recovery takes priority before merging the otherwise green #415. Hosted CI, merge and exact Production verification pending. No claim to resolve all #395 reports, retained ODsay#372 hold or full Release GO.
