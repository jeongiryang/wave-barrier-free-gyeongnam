# 이슈 #544 인트로·여행 탐색 UX — 2026-09-16

- 이슈: https://github.com/jeongiryang/wave-barrier-free-gyeongnam/issues/544
- PR: 본 문서를 포함한 이슈 #544 구현 PR
- 작성자: jeongiryang / Codex
- 최종 상태: 구현 및 로컬 검증 완료, CI 대기
- AI 도구: Codex

## 목적

기존 WAVE의 화면 구조와 전역 디자인 토큰을 유지하면서 첫 방문 인트로, 날짜 입력, 장소 직접 검색, 담기·되돌리기, 담은 장소에서 일정 만들기 흐름을 연결한다.

## 역할 구분

- 사람: #544 요구사항과 PR #466 참고 범위 결정
- AI: 최신 main 독립 작업 트리 구성, #466 인트로 범위 조사, 코드·테스트·문서 변경, 로컬 검증, PR 작성

## 구현

- PR #466 전체 디자인은 합치지 않고 인트로 사진·전환·Escape·모션 감소·1회 노출 동작만 CSS Module로 격리했다. 최종 문구는 `WAVE가 당신의 발걸음을 응원합니다`로 고정했다.
- 날짜 입력 공통 컴포넌트로 필드 전체 클릭과 Enter·Space의 기본 달력 열기, 44px 조작 영역을 플래너·축제·계정 여행·커뮤니티·사진 코스·짧은 나들이에 적용했다.
- 카카오 장소 검색을 경남 범위의 지역·관광지·카페·음식점·기타 결과로 확장했다. 운영시간·교통·편의·접근성·사진이 제공되지 않으면 값을 만들지 않고 `정보 확인 중`으로 표시한다.
- 직접 검색 결과를 기존 일정 저장 상태에 연결했다. 담은 뒤 같은 버튼이 `되돌리기`로 바뀌며, 담은 장소가 있으면 `담은 여행으로 일정 짜기`가 나타난다.
- 비어 있는 여행 탐색 화면에는 나루가 맞춤 여행을 함께 찾는다는 한 문장만 추가하고, 새 카드나 배경은 만들지 않았다.
- 사용자 화면의 `먼저` 표현과 짧은 안내 문구의 불필요한 마침표를 정리했다. 기존 전역 토큰·헤더·카드·버튼 테마는 변경하지 않았다.

## 검증

- `npm run lint -- --quiet`: PASS
- `npm run typecheck`: PASS
- `npm test`: 1,262 PASS
- `npm run build:vercel`: PASS
- `npm run check:performance`: PASS — CSS gzip 70.00/70KiB, 랜딩 초기 JS 135.71/155KiB, 플래너 초기 JS 205.38/270KiB, 최대 청크 58.14/110KiB
- 로컬 Chromium: 320·390·768·1440px 직접 검색, 미확인 정보, 담기, 같은 위치 되돌리기, 일정 버튼, 가로 넘침 없음 PASS
- 로컬 Chromium: 인트로 자동 종료·Escape·건너뛰기·모션 감소·세션 저장 차단 복구 16건 중 최초 2건의 재노출 대기 경합을 수정했고 해당 2건 재검증 PASS
- 전체 CI 1차 검증에서 직접 검색 제어가 기존 편의·장소 버튼의 CSS 선택자를 공유한 회귀를 발견했다. 검색 버튼·검색 제목·나루 안내를 독립 선택자로 분리하고, 날짜 미정 일정은 `itinerary-setup`으로 분리하면서 기존 `#itinerary` 계약과 즉시 초점 이동을 함께 보존했다.
- 보정 과정에서 드러난 마지막 9건을 포함해 데스크톱 대상 30건이 모두 통과했다. 모바일 대상은 16건 통과, 프로젝트 정책에 따른 2건 건너뛰기였고 단위 회귀 1,262건도 모두 통과했다.

## 결과와 제한

- 직접 검색의 장소 사진·운영시간·편의정보는 카카오 장소 API가 제공하지 않으므로 확인되지 않은 상태로 명시한다. 임의 이미지나 운영정보를 만들지 않는다.
- 보정 커밋의 전체 브라우저 shard와 GitHub CI를 다시 통과시킨 뒤 병합·Production 검증한다.
