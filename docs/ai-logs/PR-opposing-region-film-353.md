# 두 줄 지역 필름과 투명한 사진 위 안내

- Owner 승인: 2026-09-11 추가 사진5장과 후속 디자인 요구. Refs #353.
- 구현: Codex. 독립 검수: 별도 QA 에이전트.
- 기존 Eight 사진 장면과 Ten 타이포/여백, 단계별 Planner, 한국어·기본 테마를 보존한다.

## 바뀐 사용자 경험

18개 지역의 사진 카드를9장씩 두 줄에 배치한다. 충분히 넓고 높은 화면에서는 여행 준비 장면 마지막에 두 줄이 드러나고, 아래로 스크롤하면 위 사진들은 오른쪽·아래 사진들은 왼쪽으로 흐른다. 위로 스크롤하면 반대로 돌아가며 구간이 끝나면 다음 소개로 이어진다. 작은 화면과 모션 감소에서는 두 줄을 각각 직접 가로로 탐색한다. 모든 지역의 전체 사진 앨범, 이전/다음 지역, 일시정지, 지역별 여행 설계 링크를 유지한다.

사진 저작자·한국관광공사·원본 링크는 사진 위에서 읽을 수 있게 두고 흰 하단 블록을 제거한다. Hero/장면의 기존CC 원본·라이선스 글자도 별도 배경 없이 유지한다. 사진 전체의 어두운 그라데이션과 글자 그림자로 읽힘을 보완한다. 우측 섹션 목록과 모바일 이동 선택은 투명하며, 각 항목이 실제로 놓인 사진/밝은 구간을 읽어 글자색을 바꾼다. WAVE 글자는 보존하고 보라색 세 막대 장식만 제거한다.

## 직접 확인한 보정

- 자동 앨범 전환이 필름의 가로 위치를 덮어써12초 뒤 점프하던 문제를 재현했다. 스크롤이 위치를 제어하는 동안 자동 전환은 위치를 옮기지 않고, 사용자가 지역을 고르면 직접 탐색에 우선권을 준다.
-320px에서 긴 사진 출처가 두 줄로 늘며 사진 선택 버튼 하단을 가리던 문제를 재현했다. 모바일 선택기·제목·여행 시작 버튼과 출처의 세로 공간을 분리했다. 버튼 아래5px 클릭과 출처 영역과의 기하 간격을 검사한다.
- 투명 rail의 색을 전체 목록 중앙 한 곳으로 판단하면 서로 다른 구간에 걸친 항목이 읽히지 않았다. 항목별 실제 배경으로 판정하고 장면 전환·화면 크기·모션 감소 변경에 맞춰 갱신한다. 모션 감소에서도 남던 reveal transition을 즉시 표시로 바꿨다.
- 사진 탐색은 현재/다음 앨범만 미리 준비한다. 두 줄의 다른 지역 사진은 native lazy 이미지이며, 새 계약에 맞춰 실제 화면 이미지와 speculative Image() 요청을 별도로 검증한다. 데이터 절약은 미리 가져오기를 하지 않는다.

## 검증과 범위

현재까지 관련 E2E: 첫 실행45/52PASS 후 투명rail 대비와18카드에 맞는 프리로드 계약을 보정했다. 수정 후 Owner 장면16개 및 static320/390 4개 PASS. 필름/앨범/모바일 출처 터치 보정20개 PASS. 기존 assertion/시간제한/접근성/성능 예산을 낮추지 않았다. 실제 KTO 사진으로 독립1440/1366/960/390/320 검수 중이며 발견사항은 위에 기록했다. 최종 main 통합, 필수 로컬검사, CI, 독립 재검수와 Production 결과는 후속 기록한다.

최신 Owner 범위: 기능 후보 구현·프론트 통합·머지·정상 배포 확인까지 계속한다. 전체 조작 감사, 회귀 분석/리팩토링, 제출 전 점검은 다음 작업으로 미룬다. 카카오 메시지·운영 사용자 데이터 쓰기·결제·최종 공모전 제출은 실행하지 않는다.

최종 독립 QA PASS: 실제12.3초 앨범 자동 전환 뒤 하단위치0유지,2px세로이동에만0→4로연결.1440/1366 스크롤0/.5/1/역방향 모두 반대 방향 일치.960/390/320 수동 탐색과 모든 앨범·사진·출처·Planner링크 유지.320 긴출처60.5px와선택기사이13.5px여백, 버튼50/80/95%hit와아래3px클릭PASS. 사진위흰색/밝은구간짙은색/경계개별전환,모든caption배경투명,CC링크,WAVE글자유지. 오류·가로넘침0. 증거 `D:/wave-completion-20260911/opposing-film-qa-final-desktop.json`, `opposing-film-qa-fixed-mobile.json`, `opposing-film-qa-fixed-realtime-timer.json`, `opposing-film-qa-final-captions-rail.json` 및PNG.

최신 main #474 `c333a1b`를 정상 통합했다. 상태 문서의 같은 Owner 보류 지시 두 표현만 충돌하여 최신 문구로 보존했고 구현 충돌은 없었다. Vercel build와성능PASS: CSS69.88/70KiB, Landing117.32/155KiB, Planner176.99/270KiB. 나머지 최종 검사와 CI/Production은 아래에 이어 기록한다.

최종 로컬 typecheck, lint(오류0·기존경고12), unit810, Vercel build/performance PASS. 최신 main 포함 관련 브라우저56개 PASS. 테스트 전용 Image() 계측값의 타입 선언을 명시하고, 다음 앨범도 실제로 미리 준비되었는지 assertion을 강화한 뒤 해당2개 PASS. 독립 QA와 함께 변경 영역 검증 완료. 필수 hosted CI와 Production은 PR 뒤 확인한다.

Hosted CI34586121677에서 모바일 도움말이 가로 화면 밖의 선택 카드를 강조하는 실제 결함과, 고정 투명 안내 뒤 사진 대신 body 바탕색을 읽는 대비 계측 오류를 재현했다. 도움말은 가로 목록을 먼저 맞추고 실제 제목을 강조하며, 설명 중 필름·앨범 전환을 멈춘다. 중첩 스크롤도 강조 위치에 반영한다. 대비 helper는 fixed 투명 레이어의 실제 뒤 요소를 hit-test해 합성한다. 기존 사진/그라데이션 판정 보류 규칙과4.5기준은 그대로이며, 밝은 배경+흰글자/반투명 부족 배경을 여전히 실패시키고 어두운 배경을 통과시키는 독립 합성 사례를 추가했다. 기존 실패 두 파일14개 모두 로컬PASS. 사진 위 rail 실제 읽힘은 독립 시각 검수 대상이다.

후속 typecheck/lint/unit810/Vercel build/performance 및 합성 대비2개 PASS. 독립390/320 KO/EN 도움말의 실제 제목/강조/다이얼로그 정렬, 가로 넘침0·오류0 재확인PASS. 필름 정지·이전/다음·Escape 추가 확인과 새 HEAD hosted CI 후 병합한다.

독립 추가 확인에서320px 고정 헤더가 강조 제목을 덮는 문제를 찾아, 실제 헤더 하단+16px로 스크롤 여백을 계산했다. 제목이 헤더 아래에 있는지 assertion을 추가한 도움말8개와 typecheck/lint/unit810/build/performance PASS. 320/390 KO/EN에서 실제 제목15.5–16.4px여백·hit-test STRONG, 이전/다음/Escape·트리거 초점복귀, 넘침·오류0을 독립 확인했다. 1440 normal-motion 도움말 중4.4초 및40px세로스크롤에도 사진과두rail위치유지PASS. 근거 `D:/wave-completion-20260911/help-478-qa-followup-*`. 사진/출처/투명 안내의 앞선 검수 결과도 유지한다.
