# Restore the needs story composition at tablet widths — #353

The Owner authorized active design improvement using references, tested merges and subsequent Production verification. Codex inspected the current landing and usage screens and repaired this separately reproducible composition defect. Owner approval covers the work; final release readiness is not implied.

At 960px on actual main #431, the needs story placed its copy and portrait in the left grid column, leaving the right half empty and stretching the chapter vertically. An older tablet override assigned the portrait to column one/automatic row while the chapter still used two columns. The desktop portrait now explicitly occupies column two and the conflicting tablet override is removed. At 650px and below, the existing stacked composition explicitly puts the portrait in column one above the copy. The selected states, bounded motion, assets and source metadata are unchanged.

The existing [Design Reference Bible](../design/design-reference-bible.md), chapter 06, applies the asymmetric editorial relationship between a person-centered visual and real needs-selection DOM. The Owner's [Hanwha Ocean](https://www.hanwhaocean.com/) reference was revisited during the surrounding design review: narrow full-height imagery and the expanding photo composition were observed; the CUA desktop resize/reset timed out, so that reference viewport is not claimed inspected. This change corrects the responsive composition with the existing CSS rather than introducing a new dependency.

Verification on actual main #431: lint, typecheck, all 762 unit/contract tests, Vercel build and performance budgets passed (Planner 269.34/270 KiB, CSS 69.89/70 KiB). All 22 existing compact-landing, cinematic and theme-contrast browser cases passed in 38.7 seconds. Fixture-based captures at 390, 650, 768, 960, 1024 and 1440px reported zero horizontal overflow and zero scoped axe violations. The actual 390/960/1440px final captures and the 960px baseline were visually inspected; other widths have numerical/axe verification only. At 960px the copy and portrait occupy separate columns; mobile remains stacked. The first temporary capture helper incorrectly omitted an explicit browser context required by AxeBuilder; that helper error was corrected before the successful captures and is not a product failure. Static screenshots use reduced motion; the existing cinematic tests cover the motion contract.

No PR yet. Integrate any intervening main, repeat required checks, and verify this PR's own hosted CI, main CI/CD and canonical Production before completion. #353/#385 and the complete design acceptance scope are not closed by this CSS repair. No external provider or public-write operation was used in local verification.
