# PR #307 AI 작업 로그

- PR: https://github.com/jeongiryang/wave-barrier-free-gyeongnam/pull/307
- 제목: fix: 기존 일정 진입과 핵심 화면 위계를 바로잡는다
- 작성자: unknownamed
- 최종 상태: Draft, GitHub CI·Preview 검수 대기
- AI 도구: Codex

## 목적

운영 여정 개편 PR #287 위에서 기존 일정 진입 회귀를 막고, 실제 제공하지 않는 지도 측정
조작과 장식 전용 인트로 조작을 기본 화면에서 제거한다. Apple 참고 문서와 국내 서비스
레퍼런스를 W.A.V.E의 Deep Ocean·한국어·접근성·데이터 신뢰 기준으로 재해석한 최종 실행
명세를 함께 남긴다.

## 역할 구분

- 사람: 전체 개선 기준, 실제 코드와 PR까지 완료하라는 승인, 병합·운영 배포 최종 승인
- AI: 저장소·이슈 #252~#286·연결 PR·운영 화면·공식 레퍼런스 조사, 코드·CSS·테스트·문서
  변경, 로컬 검사, branch/commit/draft PR 생성
- 사람이 추후 직접 검토: #287과의 병합 순서, Vercel Preview, 실제 API 환경, iPhone/Android
  실기기, 운영 배포

## 검증

- `npm run lint`: 통과
- `npm run typecheck`: 통과
- `npm test`: 280/280 통과
- `npm run build:vercel`: 통과, Nitro Vercel nodejs22.x 산출물 생성
- `npm run check:performance`: 통과
  - CSS gzip 68.5 KiB / 70 KiB
  - landing initial JS gzip 112.57 KiB / 155 KiB
  - planner initial JS gzip 265.39 KiB / 270 KiB
  - largest JS chunk gzip 95.92 KiB / 110 KiB
- `npm run test:e2e`: 브라우저 시나리오 진입 전 환경 실패
  - Playwright Chromium v1234 실행 파일이 없었음
  - `npx playwright install --with-deps chromium`은 컨테이너의 apt 사용자 전환 권한
    제한(`setgroups`, `setegid`, `seteuid`)으로 실패
  - 제품 기능 실패나 테스트 통과로 기록하지 않고 GitHub CI 재실행 대상으로 남김
- 운영 화면: 공개 랜딩·플래너를 비파괴적으로 열람. 변경 branch의 실제 1440/960 화면은
  로컬 브라우저 설치 제한으로 미검증

## 결과와 제한

- 복원 가능한 기존 이 기기 일정은 새 검색 전에도 일정·출발 확인 단계에 진입한다.
- 지도에서 항상 비활성인 측정 UI를 제거하고 `지도 표시`로 실제 동작을 명확히 했다.
- 소개의 장식 재생 버튼·강제 전체화면·3D 이동·과도한 반투명을 줄였다.
- 저장 key, 공유 URL, API/DB 계약, 사용자 데이터는 변경하지 않았다.
- PR은 #287에 의존하는 stacked draft이며 단독으로 main에 먼저 병합하면 안 된다.
- 운영 배포·계정·글·좋아요·신고·공유 데이터 생성은 수행하지 않았다.
