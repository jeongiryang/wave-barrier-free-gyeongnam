# Simple is Best 개편 — AI 작업 로그

- 브랜치: `feat/simple-wave-20260913`
- 작성자·제품 결정: 정이량 (`jeongiryang`)
- AI 도구: Codex, 역할을 나눈 독립 QA 에이전트
- 상태: 로컬 최종 후보 검증 완료. PR의 필수 CI와 main CD·Production 결과는 해당 PR에 이어 기록한다.

## 목적과 결정

사용자가 기존 여행 설계의 긴 단계, 중복 카드·설명, 추상적인 문구와 나루의 실행 버튼 중심 대화를 지적했다. 협의한 `Simple is Best` 기준으로 여행지 찾기와 내 일정 두 화면을 구현하고, 나루 캐릭터를 유지했다. 소개 첫 화면은 사용자가 선택한 설명 옆 풍경 사진 배치다. 이 배치의 소스 변경은 `a141efb`에 분리했다.

사람은 제품 방향과 실행·배포 범위, 무료 DSW 모델 사용을 결정했다. AI는 코드·문서 수정, 합성 회귀·접근성·성능 검사와 실제 브라우저 조작을 수행했다. 계정·네트워크의 새로운 권한이나 유료 서비스를 추가하지 않았다. 기존 Owner 리뷰 면제를 따르되 실패하거나 대기 중인 필수 CI를 우회하지 않는다.

## 변경

- 전체 폭 공통 메뉴와 두 개의 여행 설계 탭, 상세·편의·수정·출발 정보를 필요할 때 여는 구조로 정리했다.
- 소개를 설명/사진, 지역 격자, 여행 설계 예, 나루 소개 네 구역으로 줄였다. 가로 필름을 없애고 사진·출처, 6개에서 18개 지역 펼치기, 동작 감소와 비차단 인트로를 유지한다.
- 날짜 없는 지역 탐색, 16개 개별 시설 조건, 실제 확인·미확인·부재·제공처 오류 구분과 조건을 보존하는 재시도를 구현했다.
- 나루와 직접 편집에 같은 일정 명령을 적용한다. 구체적인 수정과 되돌리기, 새 제안 검토·승인, 늦은 응답·반복 적용 방지, 실제 음량을 사용하는 음성 입력과 전송 전 확인을 제공한다.
- 안정적인 여행 ID로 저장·복원·자동 갱신을 연결한다. 새 공개 링크의 갱신·종료와 기존 스냅샷 링크의 만료를 구분하고 개인 편의·메모·GPS 출발지는 공개 자료에서 제외한다.
- 미사용 영화형 소개 스타일을 번들에서 제거했다. 일정 편집기는 처음 열 때 불러오고 이후 탭 이동에서 상태를 유지한다.

## 검증

| 검사 | 로컬 결과 |
| --- | --- |
| `npm test` | 1,210 PASS, 실패·skip 0 |
| `npm run typecheck` | PASS |
| `npm run lint` | PASS, 오류 0·경고 16 |
| `npm audit --audit-level=moderate` | 취약점 0 |
| `npm run build:vercel` | PASS |
| `npm run check:performance` | PASS, 기존 예산 변경 없음 |
| CSS gzip | 68.65 KiB / 70 KiB |
| 소개 초기 JS gzip | 128.23 KiB / 155 KiB |
| 여행 설계 초기 JS gzip | 199.59 KiB / 270 KiB |
| 최대 JS chunk gzip | 57.65 KiB / 110 KiB |

브라우저 검사는 desktop/mobile, 320–1440px, 키보드·44px·대비·초점·동작 감소, 시설·날짜·경로·저장·공유·나루·음성의 실패와 복구를 포함한다. 기존 검사를 새 두 화면의 실제 조작에 맞춰 이관했고 필수 조건·정확한 날짜·요청 횟수·오류와 원본 보존 assertion을 유지했다. 로컬에서는 에이전트별 한 worker와 재시도 0으로 검사했다. 최종 전체 브라우저 검증은 PR의 desktop/mobile 각각 네 shard에서 수행한다.

QA에서 지연 모듈이 열린 뒤 편집의 자동 저장 누락, 이동수단 변경의 중복 조회, 다른 날짜의 경로 출발점 재사용, 모바일 도움말의 늦은 대상과 초점, 어두운 화면 삭제 버튼·밝은 화면 오디오·사진 미로딩 대비를 재현해 수정했다. 자동 저장은 원본과 같으면 쓰기를 생략하고 실제 편집만 같은 ID·revision으로 반영한다. 저장소 오류가 나면 적용 전 원본과 수정 초안을 보존한다.

실제 로컬 브라우저에서는 공개 관광지로 만든 창원 일정과 실제 나루 요청을 사용했다. `창원의 집은 45분만 머물게 해줘`가 해당 장소의 체류만 60분에서 45분으로 바꿨고, 본포수변생태공원의 90분 체류와 날짜는 유지했다. 대화를 다시 열어 되돌린 뒤 기존 여행 ID의 저장 내용도 60분으로 자동 갱신되는 것을 확인했다. 설명/사진 배치, 모바일 전체 화면 대화, 시간표/지도와 실제 사진을 육안으로 확인했다.

## 배포 영향과 제한

- `013_live_shared_trips.sql`은 기존 공유 일정 테이블에 기본값을 가진 컬럼을 추가한다. 기존 링크는 `live=false`와 기존 만료일을 유지한다. 새로운 환경 변수나 유료 공급자는 없다.
- Production은 기존 GitHub Actions CD만 사용한다. 최종 PR의 전체 검증 성공 후 병합하며 main에서는 동일 tree 증명이 있는 경우에만 브라우저·경계 결과를 재사용한다.
- 전체 개편 롤백은 배포된 PR 변경을 되돌리는 후속 PR과 같은 CD 경로로 진행한다. 추가된 DB 컬럼은 기존 코드와 호환되므로 롤백 때문에 사용자 데이터를 삭제하지 않는다. 첫 화면 배치의 검토 범위는 `landing-split.css`와 `LandingHero`·`LandingHeroCopy`다.
- 실제 관광 제공처가 요청 제한을 반환한 경우를 확인했다. 오류를 정상 빈 결과로 표시하거나 시설 조건을 자동 완화하지 않는다. 이 제한과 로컬 도메인에서 지도 SDK 대신 대체 지도를 사용한 결과는 합성 API 검증과 구별한다.
- 계정 인증·공유 충돌·음성 권한/입력 신호는 통제한 합성 회귀로 확인했다. 실제 사용자의 계정 로그인 및 물리 마이크 녹음을 대신 수행했다고 주장하지 않는다. 전체 다국어·공개 다크 모드, 후속 커뮤니티 AI 기능의 완료 기록으로도 사용하지 않는다.
