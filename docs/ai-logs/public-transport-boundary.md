# 공공교통 제공처 경계 AI 작업 로그

- 작성자: jeongiryang / Codex Engineering·QA
- 범위: Refs #277 #282. 인증키 선택과 실행하지 못한 도착정보의 상태.
- 상태: 로컬 검증, 미병합·미배포.

## 2026-09-06 후속 응답 검증

Production `34e6021`의 16:56:46 UTC 전체 진단은 26/27 통과했다. 추천 KO/EN·보강·사진·집중률은
회복됐고 route 검사만 실패했다. 17:02:44 cache MISS 응답은 실제 Kakao/ODsay 대안 5개와
정류장 6개를 포함하지만 도착 0건·KORAIL 0건을 ready로 표시했다. 17:04 추가 확인에서
도착정보 문구는 "현재 조건의 결과가 없음"이었다. 이는 앱의 해석이며 원 제공처 응답의 독립 검증은 아니다.

공식 [KORAIL 열차운행정보](https://www.data.go.kr/data/15125762/openapi.do)의 내장 Swagger와
[TAGO 버스도착정보](https://www.data.go.kr/data/15098530/openapi.do)의 응답 명세를 대조했다.
둘 다 결과 코드·전체 건수·항목을 제공한다. KORAIL이 다른 최상위 data 배열 형식이라는 가설은
공식 명세로 지지되지 않아 적용하지 않았다.

현재 공용 관광 파서는 `{}`·코드 누락·구조 오류도 0건으로 바꿨다. 전용 교통 파서를 추가해
성공 코드, 명시적 음이 아닌 정수 건수, 항목 구조와 건수 일치를 검사한다. 정상 0건과
단일/복수 항목은 보존한다. 잘못된 응답은 error로 전파하며 원문 응답을 오류 안내에 넣지 않는다.
관광 파서·Production smoke의 connected 조건·timeout·재시도·기존 assertion은 변경하지 않았다.
ready를 무조건 허용해 운영 검사를 통과시키지 않는다. 운영 배포 후 검증된 빈 결과와 미조회가
기계적으로 구분되는 계약까지 확인해야 하며 현재 전체 Production 성공으로 판정하지 않는다.

- 수정 전 관련 8 pass / 2 fail → 수정 후 **10/10 PASS**.
- malformed JSON·누락 코드/건수·음수/소수/문자 건수·불일치·잘못된 항목·인증 오류를 거부한다.
- 실제 fetch→정류장 snapshot→도착 호출/상태 전파를 합성 응답으로 검증했다. 미조회와 실제 조회 0건도 구분한다.
- clean install, lint/typecheck, unit·contract **290/290**, Vercel build/performance PASS.
- source audit는 #309 미포함 기준이며 통합 audit 0과 구분한다. 전체 브라우저·새 CI는 후속 확인한다.
- Secret 사용·출력, 운영 데이터 변경, 모델 API 실행 없음. 새 PR 대신 기존 #316에서 보완한다.

## 근거와 수정

공통 helper가 TAGO → KORAIL → 공통 키 순서로 하나를 골라 모든 제공처에 보냈다.
전용 키가 서로 다를 때 KORAIL 요청에 TAGO 키가 사용됐고, 한 제공처만 설정해도
다른 제공처 요청을 실행했다. 실제 TypeScript 모듈을 합성 fetch로 실행해 재현했다.

제공처를 필수 인자로 전달하고 자기 전용 키 또는 공통 공공데이터 키만 사용한다.
키가 없는 제공처는 호출하지 않으며 health 설정 판정도 두 제공처를 함께 확인한다.
정류장 조회 실패로 실행할 수 없는 도착정보에는 의존 실패를 전파한다. 정류장 빈 결과는
오류로 바꾸지 않고 실제로 도착 API를 호출하지 않았다고 설명한다.

## 검증

- 초기 행동 회귀: **1 pass / 3 fail** → 수정 후 4/4.
- KORAIL 전용·TAGO 전용·공통 키·정류장 실패·빈 결과 등 최종 행동 회귀 **6/6**.
- 전체 단위·계약 **286/286**, lint·typecheck, Vercel production build, 성능 예산 PASS.
- 전체 Playwright·axe **239 pass / 기존 skip 1**, 실패 0.
- 테스트는 실제 모듈을 실행하되 fetch는 합성 응답만 사용했다. 실제 인증값 사용·출력 없음.

## 운영 제한

Production `34e6021`의 KORAIL/TAGO 지연과, 호출하지 않은 도착정보가 ready인 응답을
확인했다. 이 변경이 현재 상류 timeout 원인을 해결했다고 주장하지 않는다. 특히 운영은
공통 키를 쓰는 것으로 알려져 있어 전용 키 충돌 재현과 운영 장애 원인을 구분한다.
새 배포 후 제공처별 응답·실패 복구·도착정보 실제 조회를 다시 확인해야 한다.
필수 사람 승인 3건·008 점검·#309 보안 통합과 유료 모델 API 차단을 유지한다.
