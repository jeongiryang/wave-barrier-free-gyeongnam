.wave-intro { position:absolute; inset:0; width:100%; height:100%; overflow:hidden; }
.wave-intro {
  position: fixed;
  inset: 0;
  width: 100%;
  height: 100%;
  min-height: 100svh;
  overflow: hidden;
  background: #020817;
}
.wave-intro {
  isolation: isolate;
  z-index: 100;
  background: transparent;
  cursor: default;
  touch-action: none;
}
.wave-intro[data-complete="true"] {
  visibility: hidden;
  pointer-events: none;
}
.wave-intro__canvas {
  position: absolute !important;
  inset: 0;
  z-index: 1;
}
.wave-intro__static-fallback {
  position: absolute;
  inset: 0;
  background: radial-gradient(circle at 50% 58%, #0a4d94 0, #03152f 32%, #020817 72%);
}
.wave-intro__edge-glow {
  position: absolute;
  inset: 0;
  z-index: 2;
  pointer-events: none;
  background:
    radial-gradient(circle at 50% 52%, transparent 0 27%, rgb(0 75 170 / 7%) 58%, rgb(0 0 0 / 52%) 100%),
    linear-gradient(180deg, rgb(12 62 135 / 5%), rgb(0 3 14 / 32%));
  box-shadow: inset 0 0 12vw rgb(0 0 0 / 52%);
  mix-blend-mode: multiply;
}
.wave-intro__copy {
  position: absolute;
  inset: 0;
  z-index: 3;
  display: grid;
  place-items: center;
  pointer-events: none;
  text-align: center;
}
.wave-intro__subtitle {
  margin: clamp(1.15rem, 3.6vh, 2.35rem) 0 0;
  color: rgb(239 250 255 / 96%);
  font-size: clamp(1rem, 2vw, 1.7rem);
  font-weight: 550;
  letter-spacing: 0.045em;
  text-shadow: 0 0 20px rgb(35 168 255 / 85%);
  opacity: 0;
  transform: translateY(6px);
  filter: blur(4px);
  transition: opacity 180ms ease-out, transform 180ms cubic-bezier(0.22, 0.72, 0.24, 1), filter 180ms ease-out;
}
.wave-intro__subtitle-anchor {
  position: absolute;
  top: calc(50% + min(9.5vw, 12vh));
  width: 96%;
}
.wave-intro__icon-caption {
  position: absolute;
  top: calc(50% + min(38vw, 36vh));
  width: 94%;
  margin: 0;
  color: #e9f8ff;
  font-size: clamp(1rem, 1.75vw, 1.5rem);
  font-weight: 500;
  letter-spacing: 0.045em;
  text-shadow: 0 0 18px rgb(35 168 255 / 60%);
}
.wave-intro__subtitle-anchor .wave-intro__subtitle { margin:0; }